import Game from './Game.js'
export default class {
  constructor({columns = 100, rows = 100} = {}) {
    gameId : null;
    foodId : null;
    this.game : new Game({columns, rows} = {});
  }
  start(){
    this.gameId = 
  }
}
